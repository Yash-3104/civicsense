# Comma-separated frontend origins allowed to call the AI service.
AI_CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Docker Compose mounts the shared uploads volume here for local async image workflows.
UPLOADS_DIR=/app/uploads
